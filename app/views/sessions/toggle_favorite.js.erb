var sessionId = '<%= @session.id%>'

function toggleLikeButton(sessionId, innerHTML) {
  let likeButton = document.querySelector(`.like-icon[data-id='${sessionId}']`)
  likeButton.classList.toggle('red');
}

toggleLikeButton(sessionId);
